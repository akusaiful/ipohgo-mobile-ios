
class ApiService {
  // Future<Place> getNumber({int num = 20}) async {
  //   http.Response res = await http
  //       .get(Uri.http("dummyresponse.pythonanywhere.com", "/api/$num"));
  //   return Place.fromJSON(json.decode(res.body));
  // }

  // Stream<Number> getNumbers(Duration refreshTime) async* {
  //   while (true) {
  //     await Future.delayed(refreshTime);
  //     yield await getNumber();
  //   }
  // }
}
